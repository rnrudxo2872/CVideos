extends layout/main

block content 
    .form-container
        form(action=routes.users+routes.changePassword, method="post")
            input(type="password", name="oldPassword", placeholder="Insert Current Password")
            input(type="password", name="newPassword", placeholder="Insert New Password")
            input(type="password", name="enwPassword1", placeholder="Verify Password")
            input(type="submit", value="비밀번호 변경!")