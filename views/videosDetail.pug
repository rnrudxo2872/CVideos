include layout/main

block content 
    .video__player
        video(src=`/${videos.fileUrl}`) 
    .video__info
        a(href=routes.videos+routes.videoEdite(videos.id)) 비디오 수정
        h3.video__titele=videos.title
        span.video__views=videos.views
        p.video__description=videos.description
           